import 'package:flutter/material.dart';

class ParkingDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Parking Data'),
      ),
      body: Center(
        child: Text('Parking Data is here '),
      ),
    );
  }
}
